<% if @events.any? %>
  <% @events.each do |event|%>
    <h2> <%= event.title %></h2>
    <p> <%= event.location %></p>
  <% end %>
<% else %>
 <p> No events yet </p>
<% end %>

<a href="events/new">Add an event</a>