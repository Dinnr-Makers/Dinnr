<div class="row">
    <div class="toc-wrapper teal lighten-5 col s12 m2">
      <ul class="section table-of-contents">
        <li><a href="#what">What?</a></li>
        <li><a href="#when">When?</a></li>
        <li><a href="#where">Where?</a></li>
        <li><a href="#who">Who + How Many?</a></li>
      </ul>
  </div>

  <div class="form-group m6 offset-m3 col s12">
      <div id="what" class="section scrollspy">
      <h3>What</h3>
        <%= form_for @event do |f| %>
          <%= f.label :title  %>
            <%= f.text_field :title, class: 'form-control', placeholder: "Tell us what your evening's called", :required => true %>
          <%= f.label :description %>
            <%= f.text_field :description, class: 'form-control', placeholder: "Tell us a little about what you'll be cooking", :required => true %>

    <div  id="when" class="section scrollspy">
      <h3>When</h3>
        <% if @event.errors.any? %>
          <% @event.errors.full_messages.each do |message| %>
            <div class"form-error"><%= message %></div>
          <% end %>
        <% end %>
        <%= f.label :date %>
          <%= f.date_field :date, class: 'datepicker', :required => true %>
        <%= f.label :time %>
          <%= f.time_field :time, :required => true %>
    </div>

    
    <div id="where" class="section scrollspy">
      <h3>Where</h3>
      <div class="input-group">
        <div id="locationField">
          <%= f.label :location %>
            <%= f.text_field :location, class: 'form-control', id: "autocomplete", placeholder: "Tell us where we're going", :required => true %>
        </div>

        <%= f.label :housenumber, "House Number" %>
          <%= f.text_field :housenumber, class: 'form-control', id: "street_number", :required => true %> 
        <%= f.label :street %>
          <%= f.text_field :street, class: 'form-control', id: "route", :required => true %>
        <%= f.label :city %>
          <%= f.text_field :city, class: 'form-control', id: "locality", :required => true %>
        <%= f.label :postcode %>
          <%= f.text_field :postcode, class: 'form-control', id: "postal_code", :required => true %>
        <%= f.label :country %>
          <%= f.text_field :country, class: 'form-control', id: "country", :required => true %>
      </div>
    </div>

    <div  id="who" class="section scrollspy">
      <h3>Who + How Many</h3>
        <%= f.label :size %>
          <%= f.text_field :size, class: 'form-control', placeholder: "Tell us how many people you'd like to attend", :required => true %>
    </div>

    <div  id="publish" class="section scrollspy">
      <h3>Publish</h3>
    <%= f.submit 'Create Event', :class => 'btn btn-primary'%>
    <% end %>
    </div>
    </div>
  </div>
</div>